
// Example will not run with all the explaining
//console.log('Use the source luke...');
//process.exit();

var Model = require('../lib').Model;
var ProxiedModel = require('../lib').ProxiedModel;

// Create a basic model
var m = new Model();
m.on('change:foo', function(model, value) {
    console.log('New value for foo: ' + value);
    console.log(model, value);
});

console.log(m.foo);
console.log(m.get('foo'));

m.foo = 'bar';
m.foo = 'baz';

delete m.foo
